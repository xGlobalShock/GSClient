- Re-design ( stats cards ) + ( Dashboard ) - Also add a single box that has all the stats displaying inside of it

- System Tweaks + System Cleaner ( Change the borders colors to 1 simple color "Blue")
- Add a new tab on the nav bar and name it ( OBS Presets )
- Add ' Set services to manual ' to the system tweaks ( using the old project path )
- Also, i want the builder version exe file to be running as administrator by default how can we implement that?